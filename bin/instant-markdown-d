#!/usr/bin/env node

var server = require('../lib/instant-markdown-d');

server.start(8090, function(){
  var initialMarkup = '';
  process.stdin.on('data', function(chunk){
    initialMarkup += chunk;
  });
  process.stdin.on('end', function(){
    server.convert(initialMarkup);
  });
  process.stdin.resume();
});
